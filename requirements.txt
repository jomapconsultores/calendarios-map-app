Flask==3.0.0
Flask-Login==0.6.3
Flask-Session==0.5.0
google-auth==2.25.2
google-auth-oauthlib==1.2.0
google-api-python-client==2.108.0
python-dotenv==1.0.0
pytz==2023.3
gunicorn==21.2.0
requests==2.31.0
werkzeug==3.0.1
webauthn>=2.1.0,<3.0.0
openpyxl>=3.1.0
pywebpush>=2.0.0
py-vapid>=1.9.0
Flask-Limiter>=4.0.0
Flask-WTF>=1.2.0
sentry-sdk[flask]>=2.0
cryptography>=42.0
# Directorio y Cronograma
anthropic>=0.70.0        # clasificación de archivos importados y planificación del Gantt
pypdf>=5.0.0             # lectura de PDF en la importación del directorio
python-docx>=1.1.0       # lectura de Word en la importación del directorio
# Quipux / CuencaDOC — corre en la PC de la persona, no en el servidor: lee su
# bandeja del sistema documental del Municipio, baja los adjuntos y pasa los
# plazos al cronograma. La clave vive en el llavero de Windows (keyring).
keyring>=25.0            # credencial de CuencaDOC en el Administrador de credenciales
lxml>=5.0                # lectura del HTML de las bandejas y las fichas
# Sólo para la puerta: CuencaDOC levanta a veces un control de «escriba el texto
# de la imagen». Eso lo resuelve la persona en una ventana del navegador; el
# recorrido de las bandejas y las descargas siguen yendo por HTTP, que es
# veinte veces más rápido. Usa el Chrome ya instalado, no descarga otro.
playwright>=1.40         # ventana de acceso cuando salta el control de la imagen